#include <stdio.h>
#include "main.h"

/**
 * _strncpy - copies a string to a new one
 *
 * Return: thw new string
 * @dest: where to copy to
 * @src: from where to copy
 * @n: the num of string to copy
 */
char *_strncpy(char *dest, char *src, int n)
{
	return (dest);
}
