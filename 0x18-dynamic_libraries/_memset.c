#include <stdio.h>
#include "main.h"

/**
 * _memset - set an byte size for a string
 *
 * Return: the string with new size
 * @b: the bytes
 * @n: the element to change
 * @s: the string
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
