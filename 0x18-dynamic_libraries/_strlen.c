#include <stdio.h>
#include "main.h"

/**
 * _strlen - determines the length of a string
 *
 * Return: the length of the string
 * @s: the string to check its len
 */
int _strlen(char *s)
{
	return (0);
}
