#include <stdio.h>
#include "main.h"

/**
 * _strcmp - compares two strings
 *
 * Return: an int
 * @s1: the first string
 * @s2: second string to compare
 */
int _strcmp(char *s1, char *s2)
{
	return (0);
}
