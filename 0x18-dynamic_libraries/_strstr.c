#include <stdio.h>
#include "main.h"

/**
 * _strstr - string for string
 *
 * Return: the new string
 * @haystack: the accepted string
 * @needle: checked string
 */
char *_strstr(char *haystack, char *needle)
{
	return (haystack);
}
