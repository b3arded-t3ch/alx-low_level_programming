#include <stdio.h>
#include "main.h"

/**
 * _strchr - checks a string for character
 *
 * Return: a character
 * @s: the string
 * @c: the character
 */
char *_strchr(char *s, char c)
{
	return (s);
}
