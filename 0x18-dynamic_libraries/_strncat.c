#include <stdio.h>
#include "main.h"

/**
 * _strncat - concats string of n length
 *
 * Return: the new string
 * @dest: concatenated string
 * @src: from where to concat
 * @n: num of characters to concat
 */
char *_strncat(char *dest, char *src, int n)
{
	return (dest);
}
