#include <stdio.h>
#include "main.h"

/**
 * _strspn - string span
 *
 * Return: an integer
 * @s: the string
 * @accept: to be accepted
 */
unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
