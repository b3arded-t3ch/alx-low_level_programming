#include <stdio.c>
#include "main.h"

/**
 * _strpbrk - breaks a string
 *
 * Return: new string
 * @s: the string
 * @accept: to be accepted
 */
char *_strpbrk(char *s, char *accept)
{
	return (s);
}
